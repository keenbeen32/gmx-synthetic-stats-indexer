# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: synthetics-stats-hyperindex
 
# Entities are mirrored into ClickHouse alongside Postgres. Postgres stays primary:
# handlers read entity state from it and Hasura serves GraphQL from it. `default: true`
# on both backends routes every entity to both without per-entity @storage directives.
storage:
  postgres:
    default: true
  clickhouse:
    default: true

# Transaction fields needed by getOrCreateTransaction() and the SellUSDG handler.
# Block number/timestamp/hash are selected by default.
field_selection:
  transaction_fields:
    - hash
    - transactionIndex
    - from
    - to

# ---------------------------------------------------------------------------
# Global contract definitions (events + handlers shared across all chains).
# Chain sections below only attach addresses.
# ---------------------------------------------------------------------------
contracts:
  - name: EventEmitter
    events:
      - event: EventLog(address msgSender, string eventName, string indexed eventNameHash, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)
      - event: EventLog1(address msgSender, string eventName, string indexed eventNameHash, bytes32 indexed topic1, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)
      - event: EventLog2(address msgSender, string eventName, string indexed eventNameHash, bytes32 indexed topic1, bytes32 indexed topic2, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)

  # Older EventEmitter deployments (fuji, arbitrum-goerli). Same ABI/events.
  - name: EventEmitterOld
    events:
      - event: EventLog(address msgSender, string eventName, string indexed eventNameHash, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)
      - event: EventLog1(address msgSender, string eventName, string indexed eventNameHash, bytes32 indexed topic1, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)
      - event: EventLog2(address msgSender, string eventName, string indexed eventNameHash, bytes32 indexed topic1, bytes32 indexed topic2, (((string,address)[],(string,address[])[]),((string,uint256)[],(string,uint256[])[]),((string,int256)[],(string,int256[])[]),((string,bool)[],(string,bool[])[]),((string,bytes32)[],(string,bytes32[])[]),((string,bytes)[],(string,bytes[])[]),((string,string)[],(string,string[])[])) eventData)

  - name: Vault
    events:
      - event: SellUSDG(address account, address token, uint256 usdgAmount, uint256 tokenAmount, uint256 feeBasisPoints)

  - name: GlpManager
    events:
      - event: RemoveLiquidity(address account, address token, uint256 glpAmount, uint256 aumInUsdg, uint256 glpSupply, uint256 usdgAmount, uint256 amountOut)

  - name: BatchSender
    events:
      - event: BatchSend(uint256 indexed typeId, address indexed token, address[] accounts, uint256[] amounts)

  # Dynamic (factory-created) contracts — no address; registered via contractRegister.
  - name: MarketToken
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 value)

  - name: GlvToken
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 value)

# ---------------------------------------------------------------------------
# Per-chain addresses
# ---------------------------------------------------------------------------
chains:
  # arbitrum-one
  - id: 42161
    start_block: 107737756
    contracts:
      - name: EventEmitter
        address:
          - "0xC8ee91A54287DB53897056e12D9819156D3822Fb"
      - name: Vault
        address:
          - "0x489ee077994B6658eAfA855C308275EAd8097C4A"
      - name: GlpManager
        address:
          - "0x3963FfC9dff443c2A94f21b129D429891E32ec18"
      - name: BatchSender
        address:
          - "0x1070f775e8eb466154BBa8FA0076C4Adc7FE17e8"
          - "0x5384E6cAd96B2877B5B3337A277577053BD1941D"

  # avalanche
  - id: 43114
    start_block: 32162455
    contracts:
      - name: EventEmitter
        address:
          - "0xDb17B211c34240B014ab6d61d4A31FA0C0e20c26"
      - name: BatchSender
        address:
          - "0xF0f929162751DD723fBa5b86A9B3C88Dc1D4957b"
          - "0x0BEa5D3081921A08d73f150126f99cda0eb29C0e"

  # arbitrum-goerli (deprecated testnet — not on HyperSync, RPC required)
  # Skipped: legacy EventEmitterOld emits pre-release event shapes that omit
  # required fields, and this chain is RPC-only (no HyperSync).
  - id: 421613
    skip: true
    start_block: 23368123
    rpc:
      url: https://goerli-rollup.arbitrum.io/rpc
      for: sync
    contracts:
      - name: EventEmitter
        address:
          - "0x2fbE45fCb58B7106CF0a3Be9225D5Ed5A1004cc4"
      - name: EventEmitterOld
        address:
          - "0x4761b01acfa3722640c3ba021ac2d0d40f2f439e"
      - name: BatchSender
        address:
          - "0x1070f775e8eb466154BBa8FA0076C4Adc7FE17e8"

  # avalanche-fuji (testnet) — skipped for now (legacy EventEmitterOld shapes)
  - id: 43113
    skip: true
    start_block: 23300000
    contracts:
      - name: EventEmitter
        start_block: 23600000
        address:
          - "0xc67D98AC5803aFD776958622CeEE332A0B2CabB9"
      - name: EventEmitterOld
        start_block: 23300000
        address:
          - "0x5973b2B2352AE3E3fa6B86AAc74c26512Ca4c433"
      - name: BatchSender
        start_block: 23600000
        address:
          - "0x0000000000000000000000000000000000000001"

  # megaeth-mainnet (uses HyperSync)
  - id: 4326
    start_block: 5661063
    contracts:
      - name: EventEmitter
        address:
          - "0xAf2E131d483cedE068e21a9228aD91E623a989C2"
